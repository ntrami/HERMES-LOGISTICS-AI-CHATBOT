FROM node:18-alpine as builder
WORKDIR /app
COPY frontend ./frontend

# No build step needed; keep stage for extensibility

FROM nginx:alpine
COPY --from=builder /app/frontend /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]


